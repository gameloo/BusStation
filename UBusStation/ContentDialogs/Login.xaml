<ContentDialog
    x:Class="UBusStation.ContentDialogs.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UBusStation.ContentDialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
     Title="Вход в учетную запись"
    PrimaryButtonText="Войти"
    SecondaryButtonText="Отмена"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Name="TbUsername" Grid.Row="0" Margin="0,5" PlaceholderText="mail или номер телефона"/>
        <PasswordBox Name="PbPassword" Grid.Row="1" Margin="0,5" PlaceholderText="Пароль"/>
    </Grid>
</ContentDialog>
